import { Typography } from '@mui/material';

export default function TeamList() {
  return (
    <div>
      <Typography variant="h4">Team List</Typography>
      {/* Your Team list content */}
    </div>
  );
}